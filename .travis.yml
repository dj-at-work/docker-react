language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t djone/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true djone/docker-react npm run test -- --coverage